export const metadata = {
  title: 'Contact me',
  description: 'Send me a message directly to my e-mail or check my social media profiles.',
};

function Layout({ children }) {
  return children;
}

export default Layout;
